[Definition]
# Based on typical sshd failures but tuned for lab examples
failregex = ^%(__prefix_line)s(?:auth|sshd)\[\d+\]: Failed password for (?:invalid user )?.+ from <HOST> port \d+ ssh2
            ^%(__prefix_line)s(?:auth|sshd)\[\d+\]: PAM \S+\(sshd:auth\): authentication failure;.* ruser=.* rhost=<HOST>

ignoreregex =
